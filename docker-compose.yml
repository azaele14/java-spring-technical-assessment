version: '3.9'

services:
  postgres:
    image: postgres:16
    container_name: pruebatecnica-database
    environment:
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: B4nregio2803$
      POSTGRES_DB: pruebatecnica
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: